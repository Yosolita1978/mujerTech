version: 1
frontend:
  phases:
    build:
      commands:
        - "echo \"const CONFIG = { AIRTABLE_API: '$AIRTABLE_API' };\" > config.js"
  artifacts:
    baseDirectory: /
    files:
      - "**/*"